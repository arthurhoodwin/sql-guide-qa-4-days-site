# SQL Guide QA Website

Статический сайт по 4-дневному гайду SQL для QA.

## Запуск локально

Открой `index.html` в браузере или запусти простой сервер, например:

```powershell
python -m http.server 8000
```

После этого сайт доступен на `http://localhost:8000`.
